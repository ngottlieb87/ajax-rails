$("#<%= @item.id %>").remove();
$("#update_total").html("<%= number_to_currency current_order.total_price %>")
$("#cart_path").html('<%= link_to "Cart | Total items: #{ cart_items }", cart_path %>');
